---
title: "{{{title}}}"
dates:
 {{#each dates-series}}
  - "{{{this}}}"
 {{/each}}
list:
 {{#each series-item-picker}}
  - {{{this}}}
 {{/each}}
type: {{{type-series}}}
tags:
 {{#each metadata-tags}}
  - "{{{this}}}"
 {{/each}}
libraries:
 {{#each metadata-libraries}}
  - "{{{this}}}"
 {{/each}}
---

{{{body}}}
